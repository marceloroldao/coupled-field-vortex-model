# Benchmarks

Parameter sweeps and comparison baselines.

Initial benchmark axes:

- `(a,b,c)` phase-map consistency;
- `q` scans across separated / molecular / fused vortex regimes;
- axial winding `n=1,2,3` energy comparison;
- convergence versus mesh and domain size;
- comparison of spectral classifiers against full vortex solutions.
